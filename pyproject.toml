[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sts-train-perudo"
version = "0.1.0"
description = "Perudo RL training and web game project"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["src", "src.*"]
